--extra-index-url https://download.pytorch.org/whl/cu124

# Core PyTorch stack (CUDA 12.x)
torch==2.5.1+cu124
torchvision==0.20.1+cu124
torchaudio==2.5.1+cu124
# tensordict 0.6.1 is the latest release published with CUDA 12.4 wheels
tensordict==0.6.1

# Qt UI
PyQt5==5.15.11
PyQt5_sip==12.13.0

# Array & image processing
numpy==1.26.4
opencv-python==4.10.0.84
Pillow==10.4.0
scikit-image==0.24.0

# Visualization & logging
matplotlib==3.9.2
tensorboard==2.17.1
tqdm==4.66.5

# Data handling
pandas==2.2.3

# SAM2 and model tooling
# Hydra 1.2 with OmegaConf 2.2 keeps compatibility with Python 3.10
hydra-core==1.2.0
omegaconf==2.2.3
iopath==0.1.10
fvcore==0.1.5.post20221221
huggingface-hub==0.24.6
submitit==1.5.2
eva-decord==0.6.1
onnxruntime-gpu==1.19.2
pyyaml==6.0.2

# Datasets & annotations
# PyPI currently lacks Python 3.10 wheels for pycocotools-windows; rely on
# pycocotools directly and document Windows build notes in the README.
pycocotools>=2.0.8
cython==3.0.11

# Packaging helpers
setuptools>=68.0.0
wheel>=0.44.0
